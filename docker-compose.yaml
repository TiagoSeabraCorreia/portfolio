version: '3.8'
services:
  app:
    build: . 
  nginx:
    image: nginx:alpine
    container_name: portfolio-nginx
    ports: 
      - "80:80"
    volumes:
      - ./dist:/usrs/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro